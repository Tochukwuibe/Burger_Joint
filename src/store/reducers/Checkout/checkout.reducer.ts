const initialState = {
    ingredients: null,
    price: 4,
    form: null,
    loading: false,
    formIsValid: false
}

export default (state = initialState, action) => {
    switch (action.type) {



        default: {
            return state;
        }
    }
}